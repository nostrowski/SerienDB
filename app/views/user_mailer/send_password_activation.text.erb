Guten Tag <%= @user.fullname %>,

Sie haben ein neues Passwort für SerienDB-RAN angefordert.
Dies muss durch den folgenden Link bestätigt werden:
<%= url_for :controller => 'session', :action => 'password', :validation_code => @user.validation_code, :only_path => false %>

Mit freundlichen Grüßen ihr SerienDB-RAN - Team