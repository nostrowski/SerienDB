<%= form_for(@series) do |f| %>
  <% if @series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% @series.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :added_by %><br />
    <%= f.number_field :added_by %>
  </div>
  <div class="field">
    <%= f.label :edit_by %><br />
    <%= f.number_field :edit_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
